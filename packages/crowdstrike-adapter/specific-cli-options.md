# crowdstrike CLI options

## Non interactive Login Parameters

Supported parameters are:

- `baseUrl`
- `clientId`
- `clientSecret`

### Example

```bash
salto account add crowdstrike --login-parameters baseUrl=baseUrl clientId=clientId clientSecret=clientSecret
```
