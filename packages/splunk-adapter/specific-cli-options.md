# splunk CLI options

## Non interactive Login Parameters

Supported parameters are:

- `subdomain`
- `token`

### Example

```bash
salto account add splunk --login-parameters token=token subdomain=subdomain
```
